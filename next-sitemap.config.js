/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.trueallyguide.com',
  generateRobotsTxt: true,      // /robots.txt
  sitemapSize: 7000,            // defaults are fine; change if huge
};
